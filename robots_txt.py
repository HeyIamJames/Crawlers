import urlib.request
import io

def ger_robots_txt(url):
    if url.endswith('/')
        path = url
    else: 
        path - url + '/'
    req = urllib.request.urlopen(paht + "robots.txt", data=None)

# https://reddit.com/

